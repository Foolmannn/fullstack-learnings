import React from 'react'

const LineChart = () => {
  return (
    <div>LineChart</div>
  )
}

export default LineChart

// this is not accessible as it is not name page.tsx so we can keep it inside this same folder and use it through the page.tsx by importing without directly worrying exposing it . 