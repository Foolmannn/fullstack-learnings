export function BarChart(){
    return <h1>Bar Chart</h1>
}

// So we can see that only the defaul exported react component is accessible as public in the browser in next js . 

// So we can easily colocate the files in the app folder without worrying it may get revealed 

export default function Dashboard(){
    return <h1>Dashboard</h1>
}